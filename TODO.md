
## 2026-09-22: после прогона Server + Prometeus + Grafana, dda960c

[+] **BUG**: финальные значения в parallel режиме расходятся.

Исправлено в сервере и калькуляторе:  
- остановка принтера не зависит от контекста
- правильный порядок остановки: сначала калькулятор, потом принтер
- калькулятор после остановки возвращает финальные значения

[+] **api**: на `ok` сильно давит `overload`. одного счетчика `rps` явно недостаточно для анализа.
  теперь считаю все исходы: `ok`, `bad_request`, `overload` и `failed`.

[+] **metrics**: увеличить верхний порог гистограмм до 100ms (хвост оказался длиннее, чем ожидалось).

[+] **calculators**: честная FIFO очередь для `sync`
  переделал `Mutex` на `chan cap=1` (рантайм сам выстроит в очередь).

[+] **server**: опционально ограничить число соединений.
  добавлен флаг `max-conns`, по умолчанию `0` - без ограничения.

[ ] **api**: сообщить `api` в каком режиме работаем, для выбора status code: `200` (`sync`), `202` (`async` и `parallel`)

[ ] **api_test**: `wantCalls` читается неоднозначно - счетчик считает визиты в сервис, а не вызовы `Calculate`. Развести семантику или переименовать поле.

[+] **metrics**: `Printer.Print (n int, err error)` убрать `n` из ответа. `n` никому не нужно, только мешается:
- если пишем сразу в `ResponseWriter`, то поздно использовать
- если в буфер, то можем посмотреть по факту

[ ] **calculators**: отделить логику вычисления и замеров от логики режима выполнения.

[ ] **server**: 
- [  ] выпилить обертку `metricsAggr` при рефакторинге `calculators`.
- [  ] `main.go` распух. вынести `run`. при выносе учитывать возможный переход на `fasthttp`.

[+] **monitoring**: переделать дашборд Grafana в соответствии с новыми метриками:
- [+] Панель RPS за последню минуту должена отображвть новые метрики
- [+] Панель среднего значения RPS невнятная. Добавить оси, отсчет графика от 0, наложить новые метрики
- [+] Пороги для панелей "взяты с потолка": или убрать, или под реалии, или отклонение от среднего за интервал...
Переделал дашборд полностью.

[+] **generator**: 
- [+] уменьшить спам в лог генератора
- [+] сделать `startJitter` настраиваемым (зависит от среднего времени запроса + интервал)
  Добавил флаг `-jitter` (`-j`). По умолчанию `0` - поведение исходного кода на Python.
- [+] добавить флаг `duration` задающий время работы

[+] **hdr-test**: задавать количество итераций, засекать время выполнения.

[+] **docker**: *distroless* нужен только для сервера, но точные версии для сборки (повторяемость).

[+] **docker-compose**: сделать режим запуска сервера настраиваемым (например через переменные окружения).

[+] **docs**: переписать `README` **полностью** ("каша", а не `README`).

[+] **docs(calculators)**: описать семантику режимов в комментариях и README:
- `sync` - клиент ждет результата; `sum`/`sub` согласованы
- `async` - клиент не ждет; `sum`/`sub` согласованы
- `parallel` - клиент не ждет; согласованность пары в моменте не гарантируется

Go-код не вмешивается в вычисления: только инфраструктура (очередь,
ожидание, метрики). Единственное предположение - коммутативность в `parallel`.

[+] **Makefile**:
- [+] добавить зависимость целей `test` и `bench` от `build-libs` (теперь сборка не должна раздражать, библиотеки собираются по мере необходимости)
