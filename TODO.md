[+] В AsyncCalculator и asyncSingleCalculator при Stop последний батч не флашится — метрики теряются.
поставил defer в обоих serve:
```go
	var batch []metrics.Sample
	defer func() {
		if c.record != nil && batch != nil {
			c.record(batch)
		}
	}()
```

[+] ParallelCalculator может рассинхронить sum/sub, если Add прошёл, а Sub вернул ошибку.
они не возвращают ошибок. ох меня это тоже несколько напрягает, но синхронизировать порядок вычислений и Values не хочется.
для того чтобы посмотреть и так сойдет. 
Саша, загляни в код, там комментарий!

[+] SyncCalculator держит мьютекс во время record — при полной очереди метрик будет тормозить.
да, он тупой как пробка. пришли и стоят, да отзывчивость падает, но на малом количистве ядер - максимальная пропускная споcобность.
наверно стоит оганичить по количеству соединений. для сравнения оставил. основной пока Channel (async)
или я тебя не понял и ты хочешь сделать, что-то вроде?
```go
func (c *SyncCalculator) Calculate(num int64) error {
	c.mu.Lock()
	sample := c.calculate(num)
	c.mu.Unlock()

	if c.record != nil {
		c.record(sample)
	}

	return nil
}
```
зачем? record неблокирующий... хотя... нагрузка на очередь в метрик возрастает на два порядка... 
хм, может и блокирующим стать...


[+] BatchPool с unsafe.Slice и Put(&batch[0]) потенциально опасен при len=0, хотя сейчас, кажется, не стреляет.
смотрю мусье знает толк в извращениях :) содавать batch c size <= 0 - та еще идея.
```go
	if batchSize <= 0 {
		panic("batchSize must be positive")
	}
	//...
	p.pool.Put(unsafe.SliceData(batch))
```

[ ] CountRequests вызывается до валидации num — в RPS попадают и битые запросы.
да черт его знает. запрос пришел, надо посчитать. по идее надо считать и ошибки и позитивные отдельно.
но не требовалось по ТЗ. меня немного раздражает, но пока не приперло.

Опечатки: 
[+] Agregator, 
[+] GetSnapshort, 
[+] TestCalculstors, 
[-] «создание сервися». -- не нашел

[+] pprof-сервер на 6060 без обработки ошибки. -- вынесен в startPprof, все по феншую


[+] getNumFromURL не декодирует URL. -- не понял?
выкинут, т.к. влияние на производительность ничтожное

[+] metricsHandler игнорирует ошибку PrintMetrics.
ох, это да. обычно у меня middleware их ловит. в хендлере она бесполезна - заголовок уже оправлен.
да нужно залгировать по хорошему. 
-- добавил логирование в обе ручки.

[+] x/sync в go.mod, возможно, не используется напрямую.
да исчез после `go mod tidy`